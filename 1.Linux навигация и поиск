/
--/var/
----/www/
------/html/
--------/index.html
--------/404.html
--------/.htaccess
----/logs/
------/access.log

1)Выведите список всех файлов в папке html.

cd /var/www/html      #Перешли в каталог html.
find /html -type f    #Команда вывода списка всего содержимого в папке html.

2)Найдите в файле access.log все упоминания строки '404'.

grep "[404]" /var/logs/access.log        #Найдём все строки, которые содержат цифру 404

3)На основании файла 404.html сделайте файл 403.html заменив в его содержимом все упоминания 404 на 403.

cp /var/www/html/404.html /var/www/html/403.html        #Здесь копируется файл из одного каталога в другой, используются абсолютные адреса, имя копии изменяется.
cd /var/www/html                                        #Переход в директорию с файлом 403.html
sed -i 's/[404]/[403]/g' 403.html                       #Найти и заменить определенные слова в текстовом файле

4)Модифицирууйте права доступа и владельца файла index.html следующим образом:
Текущий пользователь должен иметь права на чтение/запись.
Группа www-data на чтение.
Все остальыне не имеют доступа к файлу.

cd var/www/html
ls -index.html                        #Владелец файла  может читать файл и записывать в него информацию.
-rw-rr-r--                            #Группа, тоже user, также может читать файл и записывать в него информацию.

5)Сделайте символическую ссылку на файл access.log в папке html.

ln -s /var/logs/access.log access     #Чтобы создать символическую ссылку, нужно выполнить команду ln с опцией -s:
cd var/www/html
ls -l access                          #Перенос сим-линка в директорию html.

6)Выведите на экран все файлы в папке var.

cd var
ls -m                                 #Если нужно список папок и файлов в директории через запятую

7)Создайте новый файл '503.html' в папке hmtl с следующим следующим содержимым.

cd var/www/html
echo "<html>
        <head>
            <title>503</title>
        </head>
      </html>"  503.html              #Cозданный файл 503.html будет иметь текст.

















